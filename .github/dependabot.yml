version: 2
updates:
  # Mantém as actions dos workflows atualizadas (patch/minor semanal).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
